package interfaces.service;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public interface IService {
	static final Logger log = LogManager.getLogger(IService.class.getName());
}
